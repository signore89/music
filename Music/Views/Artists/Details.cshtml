@model Music.Models.Artist

@{
    ViewData["Title"] = "Информация о исполнителе";
}

<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.UrlImg)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.UrlImg)
        </dd>
    </dl>
</div>
<div>
    @if (User.IsInRole("Admin"))// SignInManager.IsSignedIn(User) && проверяет зарегистрированный пользователь  или нет
    {
        <a asp-action="Edit" asp-route-id="@Model?.Id">Изменить</a>
    }
    <a asp-action="Index">Назад</a>
</div>
